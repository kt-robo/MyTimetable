<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/padding_items_vertical"
    android:paddingBottom="@dimen/padding_items_vertical">

    <TextView
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:id="@+id/holidayMonthTextView"
        android:layout_weight="1"
        android:textSize="@dimen/text_size_normal"
        android:textColor="@color/text_color" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:id="@+id/holidayDateTextView"
        android:layout_weight="2"
        android:layout_marginLeft="@dimen/border_in_list_size"
        android:textSize="@dimen/text_size_normal"
        android:textColor="@color/text_color" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:id="@+id/holidayNameTextView"
        android:layout_weight="5"
        android:layout_marginLeft="@dimen/border_in_list_size"
        android:textSize="@dimen/text_size_normal"
        android:textColor="@color/text_color" />
</LinearLayout>